generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model UserCredentials {
  id       String @id @default(uuid())
  username String
  password String
}
